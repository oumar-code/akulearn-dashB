# Static HTML Migration

All static HTML files have been moved to the Next.js app/static directory for integration. You can:
- Convert these files to JSX/MDX for dynamic features
- Move pure assets (images, docs) to public/static
- Use Next.js routing to serve these pages

Next steps:
- Convert key pages to React components or MDX for interactivity
- Add navigation and layout
- Test integration before deploying to Vercel
